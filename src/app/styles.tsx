import { style } from "typestyle";


export const numberPicker = style({
    'width': '4em'
});

export const statblockLeft = style({
    'fontWeight': 'bold'
});

export const transparentButton = style({
    backgroundColor: 'transparent',
    backgroundRepeat: 'no-repeat',
    border: 'none',
    cursor: 'pointer',
    overflow: 'hidden',
    outline: 'none'
});